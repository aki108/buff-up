import styled from 'styled-components/macro'
import { Typography } from 'antd'

export const Content = styled.div`
  max-width: 700px;
  background: #fff;
  border-radius: 4px;
  margin: 0 auto;
  margin-top: 1rem;
  padding: 1.5rem;
`

export const Title = styled(Typography.Title)`
  text-align: center;
`

export const AddAnswerButtonWrapper = styled.div`
  width: 60%;
  margin: 0 auto;
`
