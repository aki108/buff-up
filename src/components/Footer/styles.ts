import { Layout } from 'antd'
import styled from 'styled-components/macro'

export const StyledFooter = styled(Layout.Footer)`
  text-align: center;
  border-top: 1px solid 1px solid rgb(0 22 42 / 17%);
`
